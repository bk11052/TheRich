# 토스증권 Open API — 기초 정리

> 토스증권 Open API를 사용하기 전에 잡아둔 기초 개념 노트.
> 목적: 키 발급 후 이 API로 잔고 조회 / 시세 조회 / (신중하게) 자동매매 등을 다뤄보기 위함.

---

## 목차
1. [API란 무엇인가](#1-api란-무엇인가)
2. [어떻게 통신하는가](#2-어떻게-통신하는가)
3. [왜 요즘 중요한가](#3-왜-요즘-중요한가)
4. [Key(키)는 무엇인가](#4-key키는-무엇인가)
5. [발급 후 실제 사용 흐름](#5-발급-후-실제-사용-흐름)
6. [어디까지 가능한가 (기능과 한계)](#6-어디까지-가능한가-기능과-한계)
7. [한눈 요약표](#한눈-요약표)
8. [다음 할 일 (TODO)](#다음-할-일-todo)

---

## 1. API란 무엇인가

**API = Application Programming Interface** (응용 프로그램 사이의 약속된 창구)

**비유: 식당의 웨이터**
- 손님(내 프로그램)은 주방(토스증권 서버)에 직접 못 들어감
- 메뉴판(API 문서)을 보고 주문(요청)
- 웨이터(API)가 주문을 주방에 전달하고 음식(데이터)을 가져다줌
- 손님은 주방 내부 동작을 몰라도 됨 — 정해진 메뉴만 주문하면 됨

즉, **"프로그램끼리 정해진 규칙으로 대화하는 공식 창구"**.
토스 앱을 손으로 클릭하는 대신 **코드가 대신 토스 서버에 "내 잔고 알려줘", "이 주식 사줘"라고 말하는 방법**.

---

## 2. 어떻게 통신하는가

대부분의 웹 API는 웹서핑과 똑같은 **HTTP** 방식. 핵심은 **요청(Request) → 응답(Response)** 한 쌍.

### 요청(Request) 구성
| 구성 | 의미 | 예시 |
|------|------|------|
| **Endpoint (URL)** | 어디로 보낼지(주소) | `https://.../api/v1/balance` |
| **Method** | 무엇을 할지 | `GET`(조회) `POST`(실행/생성) `DELETE`(삭제) |
| **Headers** | 부가 정보, **API Key(신분증)** 를 넣음 | `Authorization: Bearer xxx` |
| **Body** | 보낼 데이터(주로 JSON) | `{ "종목":"NVDA", "수량":10 }` |

### 응답(Response) 구성
- **상태 코드**: `200`(성공) · `401`(인증 실패) · `404`(없는 주소) · `429`(호출 과다) · `500`(서버 오류)
- **Body**: 실제 결과 데이터, 보통 **JSON**

### JSON 예시
사람도 컴퓨터도 읽기 쉬운 데이터 표기법.
```json
{
  "종목명": "엔비디아",
  "보유수량": 10,
  "평가손익": 152000,
  "수익률": "+8.3%"
}
```

- 요청→JSON응답 구조를 따르는 API = 흔히 **REST API** (토스증권 API도 이 방식)
- 실시간 시세처럼 계속 흘러오는 데이터는 **WebSocket**(통화를 계속 연결해두는 방식)도 함께 씀

---

## 3. 왜 요즘 중요한가

1. **자동화** — 밤새 차트 볼 필요 없이 조건을 걸어두면 코드가 감시·실행 (토스의 *"잠든 사이에도 거래"*)
2. **서비스 조립(레고 블록)** — 계좌 데이터를 엑셀·노션·나만의 대시보드에 붙이는 등 서비스 연결
3. **AI 시대의 핵심** — AI가 말만 하는 걸 넘어 **실제로 행동**하려면 API가 필요.
   토스의 *"AI로 코드 없이 대화로 — 계좌를 연결해 '내 포트폴리오 수익률 분석해줘'"* 가 이 흐름.
   **AI ↔ API 연결 표준(MCP 등)** 덕분에 코드를 못 짜도 AI가 대신 API를 호출.

---

## 4. Key(키)는 무엇인가

**API Key = 신분증 + 열쇠**. 요청 Header에 실어 보내면 서버가 확인:
- **인증(Authentication)**: 누가 요청하는지 (너 진짜 계좌 주인 맞아?)
- **인가(Authorization)**: 어디까지 허용할지 (조회만? 주문까지?)
- **사용량 추적/제한**: 너무 많이 부르면 막음

증권사 API는 보통 **App Key + Secret Key 두 개 짝**을 주고, 이걸로 먼저
**접근 토큰(Access Token)** 이라는 임시 출입증을 받아 실제 요청에 사용하는 패턴이 많음.
(정확한 방식은 발급 후 **공식 문서** 확인 필수.)

### ⚠️ 보안 — 증권 계좌라서 특히 중요
이 키는 사실상 **내 계좌에 접근하고 돈을 움직일 수 있는 권한**.
- 절대 **GitHub·블로그·카톡·스크린샷**에 노출 금지
- 남의 코드/사이트에 함부로 입력 금지
- 노출 의심되면 **즉시 재발급(폐기)**
- 코드에 직접 박지 말고 **`.env` + 환경변수**로 관리 (아래 참고)

```bash
# .env 예시 (이 파일은 절대 커밋하지 말 것 → .gitignore 에 추가)
TOSS_APP_KEY=여기에_발급받은_키
TOSS_SECRET_KEY=여기에_시크릿
```

---

## 5. 발급 후 실제 사용 흐름

1. **키 발급** — 토스증권 `Open API Key 설정`
2. **인증** — 발급받은 키로 접근 토큰 받기
3. **요청** — 원하는 기능의 Endpoint로 요청
4. **응답 활용** — JSON 결과를 화면에 뿌리거나, 조건에 따라 다음 행동

### 두 갈래 방법
- **(A) 직접 코딩** — Python 등으로 요청 코드 작성. 자유도 최고. 잔고 조회는 몇 줄이면 됨.
- **(B) 코드 없이 AI에 연결** — 토스가 미는 방식. AI에 계좌 연결 후 *"내 수익률 분석해줘"* 처럼 말로 시킴. 입문자에게 편함.

---

## 6. 어디까지 가능한가 (기능과 한계)

토스증권 화면(*국내/해외 통합, 반복 매매 자동화* 등) 기준 **추정**:

### ✅ 가능 (추정)
- **조회**: 실시간 시세, 내 잔고·보유종목, 수익률, 거래내역
- **주문**: 매수/매도 **실제 실행**, 조건부 자동매매(알고리즘 트레이딩)
- **연동**: 데이터를 AI·나만의 앱·스프레드시트에 연결

### ⛔ 한계 / 주의
- **Rate Limit(호출 제한)**: 초당/일당 요청 횟수 제한. 넘으면 `429`.
- **권한 범위**: 발급 시 "조회만" / "주문까지" 등 권한 구분 가능.
- **🔴 진짜 돈이 움직임**: 주문 API는 실거래. 버그 하나로 원치 않는 매매가 나갈 수 있음
  → 처음엔 **소액 또는 모의투자(있다면)로 충분히 테스트**.
- **정확한 스펙은 공식 문서 기준**: 위 기능은 화면 기반 추정. 발급 페이지의 **API 레퍼런스**가 최종 기준.

---

## 한눈 요약표

| 질문 | 한 줄 요약 |
|------|-----------|
| API란? | 프로그램끼리 정해진 규칙으로 대화하는 창구 (식당 웨이터) |
| 어떻게 통신? | HTTP 요청→응답, 데이터는 JSON |
| 왜 중요? | 자동화 + 서비스 연결 + **AI가 실제 행동하는 통로** |
| Key란? | 신분증 겸 열쇠, **증권 계좌 접근 권한이라 절대 노출 금지** |
| 사용법? | 키 발급 → 인증 → 요청 → 응답 활용 (직접 코딩 or AI 연결) |
| 한계? | 호출 제한 · 권한 범위 · **실거래라 신중히** |

---

## 다음 할 일 (TODO)

- [ ] 토스증권 `Open API Key 설정`에서 키 발급
- [ ] 공식 API 문서(레퍼런스) 확보 후 인증 방식(App Key + Secret → Access Token 여부) 확인
- [ ] `.env` 파일 만들고 `.gitignore`에 추가 (키 노출 방지)
- [ ] Python으로 **잔고 조회** 최소 예제 작성 (첫 통신 성공시키기)
- [ ] 시세 조회 → 이후 자동매매는 소액/모의로만 신중히 검증
